spring:
  application:
    name: notification-system
  datasource:
    url: jdbc:postgresql://localhost:5432/notificatiodb
    username: gtzuc
    password: rootGtzuc
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
#  servlet:
#    context-path: /notification-system
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/